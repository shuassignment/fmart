<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="GreenTheme" parent="FirebaseUI">
        <!-- Required for sign-in flow styling -->
        <item name="colorPrimary">@color/fui_bgEmail</item>
        <item name="colorPrimaryDark">@color/fui_errorColor</item>
        <item name="colorAccent">@color/fui_bgPhone</item>

        <item name="colorControlNormal">@color/common_google_signin_btn_text_dark</item>
        <item name="colorControlActivated">@color/cardview_dark_background</item>
        <item name="colorControlHighlight">@color/com_facebook_blue</item>
        <item name="android:windowBackground">@color/com_facebook_device_auth_text</item>
    </style>

</resources>
